// CÓDIGO NÃO EXECUTÁVEL!!!

// Procedural
processamento(valor1, valor2, valor3)

// Orientação à objetos
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// Principios importantes:
// 1) Abstração: vc pegar um objeto do mundo real e abstrair para o seu conceito de software: peso, altura, metodos como ligar, desligar, frear, etc
// 2) Encapsulamento: vc ter os detalhes de implementação escondidos e mostrar para quem precisa integrar, uma interface amigável para que a pessoa consiga lidar com essas informações
// 3) Herança: formas de vc facilitar o reuso de informações encapsuladas
// 4) Polimorfismo: multiplas formas. A partir de um conceito genérico, vc pode criar conceitos concretos. Por exemplo: genérico (carro) > concreto (ferrari)